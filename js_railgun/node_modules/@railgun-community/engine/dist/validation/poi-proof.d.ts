import { Proof } from '../models/prover-types';
import { Prover } from '../prover/prover';
export type TransactProofData = {
    snarkProof: Proof;
    poiMerkleroots: string[];
    txidMerkleroot: string;
    txidMerklerootIndex: number;
    blindedCommitmentsOut: string[];
    railgunTxidIfHasUnshield: string;
};
export declare class POIProof {
    static verifyTransactProof: (prover: Prover, transactProofData: TransactProofData) => Promise<boolean>;
    private static getPublicInputsPOI;
    private static tryVerifyProof;
}
