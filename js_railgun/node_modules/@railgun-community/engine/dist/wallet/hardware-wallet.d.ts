/// <reference path="../../src/types/global.d.ts" />
import { Signature } from '@railgun-community/circomlibjs';
import { PublicInputsRailgun } from '../models';
import { Database } from '../database/database';
import { SpendingKeyPair, SpendingPublicKey, ViewingKeyPair } from '../key-derivation/wallet-node';
import { RailgunWallet } from './railgun-wallet';
import { Prover } from '../prover/prover';
export type ExternalSignerConnectorSignFn = (expectedHash: bigint, publicInputs?: PublicInputsRailgun, subSession?: string) => Promise<Signature>;
export type ExternalSignerConnector = {
    sign: ExternalSignerConnectorSignFn;
    requestBatchApproval?: (requests: readonly unknown[]) => Promise<string>;
};
declare class HardwareWallet extends RailgunWallet {
    private connector;
    private readonly storedSpendingPublicKey;
    constructor(id: string, db: Database, viewingKeyPair: ViewingKeyPair, spendingPublicKey: SpendingPublicKey, creationBlockNumbers: Optional<number[][]>, prover: Prover);
    getSpendingKeyPair(_encryptionKey: string): Promise<SpendingKeyPair>;
    setConnector(connector: ExternalSignerConnector): void;
    requestBatchApproval(requests: readonly unknown[]): Promise<Optional<string>>;
    sign(publicInputs: PublicInputsRailgun, subSession: string): Promise<Signature>;
    private static generateHardwareID;
    private static getHardwareViewingKeyPair;
    private static createHardwareWallet;
    static fromShareableViewingKey(db: Database, encryptionKey: string, shareableViewingKey: string, creationBlockNumbers: Optional<number[][]>, prover: Prover): Promise<HardwareWallet>;
    static loadExisting(db: Database, encryptionKey: string, id: string, prover: Prover): Promise<HardwareWallet>;
}
export { HardwareWallet };
