import { NetworkName } from '@railgun-community/shared-models';
export declare class POIRequired {
    private static requiredForNetwork;
    static isRequiredForNetwork(networkName: NetworkName): Promise<boolean>;
    static getRequiredListKeys(networkName: NetworkName): Promise<string[]>;
}
