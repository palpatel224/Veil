import { ExtractedRailgunTransactionData, POIMerklerootsValidator } from '@railgun-community/engine';
import { Chain, PreTransactionPOIsPerTxidLeafPerList, TXIDVersion } from '@railgun-community/shared-models';
import { ContractTransaction } from 'ethers';
export declare class POIValidator {
    static initForPOINode(validatePOIMerkleroots: POIMerklerootsValidator): void;
    static isValidSpendableTransaction(railgunWalletID: string, txidVersion: TXIDVersion, chain: Chain, transactionRequest: ContractTransaction, useRelayAdapt: boolean, pois: PreTransactionPOIsPerTxidLeafPerList): Promise<{
        isValid: boolean;
        error?: string;
        extractedRailgunTransactionData?: ExtractedRailgunTransactionData;
    }>;
    static assertIsValidSpendableTXID(listKey: string, txidVersion: TXIDVersion, chain: Chain, pois: PreTransactionPOIsPerTxidLeafPerList, railgunTxids: string[], utxoTreesIn: bigint[]): Promise<boolean>;
}
