/// <reference types="../../../types/global" />
import { Chain } from '@railgun-community/engine';
import { TXIDVersion } from '@railgun-community/shared-models';
export declare class TestWalletPOIRequester {
    validateRailgunTxidMerkleroot(txidVersion: TXIDVersion, chain: Chain, tree: number, index: number, merkleroot: string): Promise<boolean>;
    getLatestValidatedRailgunTxid(txidVersion: TXIDVersion, chain: Chain): Promise<{
        txidIndex: Optional<number>;
        merkleroot: Optional<string>;
    }>;
}
